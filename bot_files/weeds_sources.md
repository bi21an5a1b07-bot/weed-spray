# Public RGB sources (v1 survey)

Checked: 2026-08-31. No archives downloaded. Classes: `dandelion`=0, `clover`=1, `thistle`=2, `mallow`=3 (operator-added; public RGB survey is still thin on mallow). Turf is background.

There is **no** public, license-clear, US-lawn, 3-class detection set. Everything below is a building block. Counts are observations or paper tables, not “ready YOLO boxes,” unless noted.

## Use (or near-use)

See `weeds/sources.md` (canonical). This bot_files copy is kept in sync.
