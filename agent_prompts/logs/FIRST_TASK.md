Define the post-run summary format and write an empty template.

Read `/workspace/weed-spray/PROJECT.md` and `SAFETY.md`.

Outcome: `/workspace/weed-spray/logs/summaries/_template.md` with the required sections (setup, detections by class, confirms, hover 6–12 in stats, pump pulses vs default 0.75 s, failsafes, geofence, defects). Include which PX4 ulog topics or dashboard JSON keys you expect once Grok Build exists.

Constraints: no sample numbers unless I upload a log. Do not connect to anything.

Deliverable: write the template, then list the minimum fields the laptop app must log. Stop.
